# Worker Backups

- `2026-06-26-homepage-hot-tv-entry-backup.js`
  - Backup of the Worker version that added the homepage `hot_tv` pseudo-category entry.
  - Purpose: preserve the implementation that exposed Douban hot TV as a clickable category before reverting to a homepage-only recommendation strategy.
  - Status: kept for rollback/reference only; not the active Worker entrypoint.

- `backup_v1_stable.js`
  - Earlier stable Worker snapshot kept from prior iterations.

- `backup_v2_stable.js`
  - Later stable Worker snapshot kept from prior iterations.

- `worker_simple_backup.js`
  - Minimal/simple Worker backup from early iterations.

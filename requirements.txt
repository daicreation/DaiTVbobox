# ============================================================
# TVBox 影視聚合系統 — Python 依賴
#
# 安裝方式:
#   pip install -r requirements.txt
#
# 核心依賴 (無需系統層級套件):
#   httpx          — HTTP 客戶端 (支援 HTTP/2, 非同步)
#   tenacity       — 自動重試機制 (指數退避)
#   pyyaml         — YAML 配置解析
#   python-levenshtein — 高效字串相似度比對 (可選)
#   pydantic       — 數據驗證 (可選，用於更嚴格的型別檢查)
#   structlog      — 結構化日誌 (可選)
# ============================================================

# ---- 核心 ----
httpx>=0.27.0,<1.0.0          # HTTP 客戶端 (支援 HTTP/2, 重定向)
tenacity>=8.0.0,<9.0.0        # 自動重試 (指數退避)
pyyaml>=6.0,<7.0.0            # YAML 配置檔讀取

# ---- 可選: 高效字串比對 ----
# 如果安裝失敗，會自動 fallback 到純 Python 實現
python-Levenshtein>=0.25.0; platform_system != "Windows"
# Windows 上通常需要編譯，使用純 Python fallback
# python-Levenshtein-wheels; platform_system == "Windows"

# ---- 可選: 數據驗證 ----
pydantic>=2.0.0,<3.0.0        # 型別驗證 (備用方案)

# ---- 可選: CLI 美觀輸出 ----
rich>=13.0.0,<14.0.0          # 終端機彩色輸出

# ---- 開發/測試 ----
pytest>=8.0.0,<9.0.0          # 單元測試框架
pytest-cov>=5.0.0,<6.0.0      # 測試覆蓋率報告
pytest-mock>=3.0.0,<4.0.0     # Mock 輔助
